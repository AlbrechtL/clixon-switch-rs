[package]
name = "clixon-plugin"
description = "Safe Rust interface for writing clixon backend plugins"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
clixon-sys = { path = "../clixon-sys" }
